---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: harbor.haas-440.pez.pivotal.io/library/malston/pks-control-plane
    insecure_registries: [harbor.haas-440.pez.pivotal.io]
    tag: 0.0.1

inputs:
  - name: repo

run:
  path: repo/ci/tasks/upgrade-chart/task.sh

params:
  RELEASE:
  VERSION:
