---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: harbor.haas-440.pez.pivotal.io/library/malston/pks-control-plane
    insecure_registries: [harbor.haas-440.pez.pivotal.io]
    tag: 0.0.1

inputs:
  - name: prometheus-deployment
  - name: pks-config
  - name: kube-config

run:
  path: prometheus-deployment/ci/tasks/install-operator/task.sh

params:
  OPSMAN_HOSTNAME:
  OPSMAN_USER:
  OPSMAN_PASSWORD:
  OPSMAN_SKIP_SSL_VALIDATION:
  FOUNDATION:
  NAMESPACE:
  RELEASE:
  VERSION:
