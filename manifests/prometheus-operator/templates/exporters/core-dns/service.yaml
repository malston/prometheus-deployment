---
# Source: prometheus-operator/templates/exporters/core-dns/service.yaml

apiVersion: v1
kind: Service
metadata:
  name: prom-operator-prometheus-o-coredns
  namespace: monitoring
  labels:
    app: prometheus-operator-coredns
    jobLabel: coredns
    
    chart: prometheus-operator-6.20.3
    release: "prom-operator"
    heritage: "Tiller"
  namespace: kube-system
spec:
  clusterIP: None
  ports:
    - name: http-metrics
      port: 9153
      protocol: TCP
      targetPort: 9153
  selector:
    
    k8s-app: kube-dns
