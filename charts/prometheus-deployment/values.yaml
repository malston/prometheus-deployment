# Default values for prometheus-deployment.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

## Provide a name in place of prometheus-deployment for `app:` labels
##
nameOverride: ""

## Provide a name to substitute for the full names of resources
##
fullnameOverride: ""

alertmanager:
  host: ""

grafana:
  host: ""

prometheus:
  host: ""

istio:
  enabled: false

ingress:
  enabled: true
